info:
  name: testing
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/v1/add-test?test=testing2"
  params:
    - name: test
      value: testing2
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
