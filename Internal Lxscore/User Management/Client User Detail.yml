info:
  name: Client User Detail
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/v1/internal/client/{id}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
