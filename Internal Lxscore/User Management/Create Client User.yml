info:
  name: Create Client User
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/v1/internal/client"
  body:
    type: json
    data: |-
      {
        
        "name": "",
        "email": "",
        "password": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
