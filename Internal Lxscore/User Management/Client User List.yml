info:
  name: Client User List
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/v1/internal/client?limit=&after=&before=&keyword="
  params:
    - name: limit
      value: ""
      type: query
    - name: after
      value: ""
      type: query
    - name: before
      value: ""
      type: query
    - name: keyword
      value: ""
      type: query
  auth:
    type: bearer
    token: "{{internal_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
