info:
  name: Close Support Ticket
  type: http
  seq: 5

http:
  method: PUT
  url: "{{base_url}}/v1/internal/support-tickets/{id}/close"
  auth:
    type: bearer
    token: "{{internal_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
