info:
  name: Message Support Ticket
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/v1/internal/support-tickets/{id}/messages?limit=&next="
  params:
    - name: limit
      value: ""
      type: query
    - name: next
      value: ""
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
