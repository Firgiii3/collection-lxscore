info:
  name: Internal Overview
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/v1/internal/overview"
  auth:
    type: bearer
    token: "{{internal_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
