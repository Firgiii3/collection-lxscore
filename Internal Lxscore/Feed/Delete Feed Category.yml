info:
  name: Delete Feed Category
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{base_url}}/v1/internal/feed/category/{id}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
