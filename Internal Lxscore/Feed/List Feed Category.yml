info:
  name: List Feed Category
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/v1/internal/feed/category?limit=&next="
  params:
    - name: limit
      value: ""
      type: query
    - name: next
      value: ""
      type: query
  auth:
    type: bearer
    token: "{{internal_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
