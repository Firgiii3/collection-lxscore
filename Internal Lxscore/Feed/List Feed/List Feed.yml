info:
  name: List Feed
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/v1/internal/feed?limit=&next="
  params:
    - name: limit
      value: ""
      type: query
    - name: next
      value: ""
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
