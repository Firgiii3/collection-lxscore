info:
  name: Feed Detail
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/v1/internal/feed/{id}"
  auth:
    type: bearer
    token: "{{internal_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
