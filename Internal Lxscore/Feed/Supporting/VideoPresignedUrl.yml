info:
  name: VideoPresignedUrl
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/v1/storage/public/video"
  auth:
    type: bearer
    token: "{{internal_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
