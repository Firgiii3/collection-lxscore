info:
  name: Update Feed Category
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{base_url}}/v1/internal/feed/category/{id}"
  body:
    type: json
    data: |-
      {
        "body":"string"
      }
  auth:
    type: bearer
    token: "{{internal_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
