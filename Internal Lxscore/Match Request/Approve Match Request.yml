info:
  name: Approve Match Request
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/v1/internal/match-request/{id}/approve"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
