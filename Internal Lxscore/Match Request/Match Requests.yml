info:
  name: Match Requests
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/v1/internal/match-request"
  params:
    - name: status
      value: ""
      type: query
      disabled: true
    - name: next
      value: ""
      type: query
      disabled: true
    - name: limit
      value: ""
      type: query
      disabled: true
  auth:
    type: bearer
    token: "{{internal_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
