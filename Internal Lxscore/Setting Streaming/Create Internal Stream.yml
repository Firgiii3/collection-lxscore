info:
  name: Create Internal Stream
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/v1/internal/stream"
  body:
    type: json
    data: |-
      {
        "matches" :"number"
      }
  auth:
    type: bearer
    token: "{{internal_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
