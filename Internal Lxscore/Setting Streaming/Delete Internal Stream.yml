info:
  name: Delete Internal Stream
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{base_url}}/v1/internal/stream"
  body:
    type: json
    data: |-
      {
        "matches" :"number"
      }
  auth:
    type: bearer
    token: "{{internal_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
