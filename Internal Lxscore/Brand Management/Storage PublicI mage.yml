info:
  name: Storage PublicI mage
  type: http
  seq: 5

http:
  method: POST
  url: "{{base_url}}/v1/storage/public/image"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{internal_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
