info:
  name: Delete Internal Layout
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{base_url}}/v1/internal/layout/{id}?organization_id="
  params:
    - name: organization_id
      value: ""
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
