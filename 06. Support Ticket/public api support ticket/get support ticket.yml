info:
  name: get support ticket
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/v1/public/support-tickets"
  auth:
    type: bearer
    token: "{{user_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
