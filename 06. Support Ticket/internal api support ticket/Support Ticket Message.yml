info:
  name: Support Ticket Message
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/v1/internal/support-tickets/997ab6f9-6289-4c9f-9514-d97dddc3bb74/messages"
  body:
    type: json
    data: "{\r

      \  \"message\": \"Kami dari internal akan bantu cek issue ini.\",\r

      \  \"attachment\": []\r

      }"
  auth:
    type: bearer
    token: "{{internal_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
