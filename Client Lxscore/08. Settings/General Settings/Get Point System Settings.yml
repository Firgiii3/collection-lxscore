info:
  name: Get Point System Settings
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/v1/client/setting/point?organization_id="
  params:
    - name: organization_id
      value: ""
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
