info:
  name: Delete Daily Login Config
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url}}/v1/client/daily-login-configs/{id}"
  auth:
    type: bearer
    token: "{{client_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
