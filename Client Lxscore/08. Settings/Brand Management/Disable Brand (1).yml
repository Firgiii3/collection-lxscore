info:
  name: Disable Brand (1)
  type: http
  seq: 4

http:
  method: PUT
  url: "{{base_url}}/v1/client/setting/brand/{brandId}"
  body:
    type: json
    data: |-
      {
        "name": "string",
        "website": "string",
        "description": "string",
        "organization_id": "string"
      }
  auth:
    type: bearer
    token: "{{client_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
