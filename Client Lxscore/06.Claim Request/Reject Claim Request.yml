info:
  name: Reject Claim Request
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/v1/client/claim-rewards/{claimId}/reject"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
