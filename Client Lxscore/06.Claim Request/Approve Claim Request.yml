info:
  name: Approve Claim Request
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/v1/client/claim-rewards/{claimId}/approve?claimId="
  params:
    - name: claimId
      value: ""
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
