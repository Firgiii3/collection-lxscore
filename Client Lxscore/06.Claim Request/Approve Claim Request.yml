info:
  name: Approve Claim Request
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/v1/client/claim-rewards/{claimId}/approve"
  params:
    - name: claimId
      value: ""
      type: query
      disabled: true
  auth:
    type: bearer
    token: "{{client_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
