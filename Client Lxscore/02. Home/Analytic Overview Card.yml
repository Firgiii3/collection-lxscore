info:
  name: Analytic Overview Card
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/v1/client/analytic/overview"
  auth:
    type: bearer
    token: "{{client_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
