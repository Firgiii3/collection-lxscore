info:
  name: Analytic Top Performance
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/v1/client/analytic/top-performer"
  auth:
    type: bearer
    token: "{{client_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
