info:
  name: Analytic Latest Register
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/v1/client/analytic/latest-registered"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
