info:
  name: Analytic Engagement Funnel
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/v1/client/analytic/engagement-funnel"
  auth:
    type: bearer
    token: "{{client_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
