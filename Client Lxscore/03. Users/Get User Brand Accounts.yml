info:
  name: Get User Brand Accounts
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/v1/client/users/{publicUserId}/brand_accounts?limit=&next="
  params:
    - name: limit
      value: ""
      type: query
    - name: next
      value: ""
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
