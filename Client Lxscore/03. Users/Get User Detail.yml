info:
  name: Get User Detail
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/v1/client/users/detail/{publicUserId}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
