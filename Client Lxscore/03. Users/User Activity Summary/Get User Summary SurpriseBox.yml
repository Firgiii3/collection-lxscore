info:
  name: Get User Summary SurpriseBox
  type: http
  seq: 7

http:
  method: GET
  url: "{{base_url}}/v1/client/users/{publicUserId}/surprise_box"
  auth:
    type: bearer
    token: "{{client_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
