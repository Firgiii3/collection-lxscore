info:
  name: Get User Summary ClaimRequest
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/v1/client/users/{publicUserId}/claim_request"
  auth:
    type: bearer
    token: "{{client_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
