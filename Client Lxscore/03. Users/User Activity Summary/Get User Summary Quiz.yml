info:
  name: Get User Summary Quiz
  type: http
  seq: 8

http:
  method: GET
  url: "{{base_url}}/v1/client/users/{publicUserId}/quiz"
  auth:
    type: bearer
    token: "{{client_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
