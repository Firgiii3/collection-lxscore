info:
  name: Get User Summary WatchingTime
  type: http
  seq: 7

http:
  method: GET
  url: "{{base_url}}/v1/client/users/{publicUserId}/watching_time?limit=&next="
  params:
    - name: limit
      value: ""
      type: query
    - name: next
      value: ""
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
