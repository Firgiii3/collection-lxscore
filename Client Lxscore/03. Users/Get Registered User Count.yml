info:
  name: Get Registered User Count
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/v1/internal/registered"
  auth:
    type: bearer
    token: "{{client_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
