info:
  name: Create Match Request
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/v2/client/match-request"
  body:
    type: json
    data: |-
      {
        "organization_id": "1234-1",
        "match": [],
        "match_id": "string",
        "mrketing_boost": true
      }
  auth:
    type: bearer
    token: "{{client_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
