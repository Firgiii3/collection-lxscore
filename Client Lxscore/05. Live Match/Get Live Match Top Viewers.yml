info:
  name: Get Live Match Top Viewers
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/v1/client/live-match/{matchId}/top-viewer"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
