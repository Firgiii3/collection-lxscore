info:
  name: Get Live Match Detail
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/v1/client/live-match/{id}?id="
  params:
    - name: id
      value: ""
      type: query
  auth:
    type: bearer
    token: "{{client_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
