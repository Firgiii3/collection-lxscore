info:
  name: Get Live Match Detail Analytic
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/v1/client/live-match/{matchId}/analytic?matchId="
  params:
    - name: matchId
      value: ""
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
