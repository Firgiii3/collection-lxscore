info:
  name: Get Live Match Detail Analytic
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/v1/client/live-match/{matchId}/analytic"
  auth:
    type: bearer
    token: "{{client_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
