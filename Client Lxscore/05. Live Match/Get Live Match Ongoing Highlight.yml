info:
  name: Get Live Match Ongoing Highlight
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/v1/client/live-match/highlight"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
