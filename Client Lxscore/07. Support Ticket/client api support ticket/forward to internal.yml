info:
  name: forward to internal
  type: http
  seq: 4

http:
  method: PUT
  url: "{{base_url}}/v1/client/support-tickets/581d930f-97b6-4d55-acd3-4b2f48680246/forward-to-internal"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{client_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
