info:
  name: create support ticket
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/v1/client/support-tickets/581d930f-97b6-4d55-acd3-4b2f48680246/messages"
  body:
    type: json
    data: "{\r

      \  \"message\": \"Baik, kami akan cek data watching time user.\",\r

      \  \"attachment\": []\r

      }"
  auth:
    type: bearer
    token: "{{client_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
