info:
  name: get list quiz
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/v1/public/match_order/quiz"
  auth:
    type: bearer
    token: "{{user_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
